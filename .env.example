# GenieACS connection details
ACS_URL=http://localhost:7557
ACS_USER=
ACS_PASS=

# Maximum number of devices returned by genieacs://devices/list (default 500)
# DEVICE_LIMIT=500

# HTTP listen address (only used when TRANSPORT is not stdio, default 127.0.0.1:8080)
# MCP_LISTEN_ADDR=127.0.0.1:8080